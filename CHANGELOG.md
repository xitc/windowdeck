# 更新记录

## v0.4.8-beta.2

桌面选择加入和替换，以及加入动画。

- 侧边「＋」把整组收到桌面边缘。之后在桌面打开的应用成为主窗，原来的主窗移到最后一张侧卡。已经在工作台里的应用只展开回来。
- 主窗菜单「替换应用」同样回到桌面选择。新应用换上主窗，侧窗留在原位，原来的主应用离开工作台。侧卡长按里的替换仍用列表。
- 横屏上滑的分屏/浮窗面板中间也有「添加到工作台」。
- 加入时新窗口从全屏缩进主卡。挂起后加入时，原来的窗口同时从边缘回到侧卡。动画结束后恢复主窗和侧卡操作。

验证：一加 13 上做过挂起加入、替换和上滑卡片的有限测试。偶发加入失败和动画衔接仍需继续验收。仅列为测试版本。

## v0.4.7-beta.22

同步到当前已安装的测试版。

- 桌面竖屏上滑面板增加中间项“添加到工作台”。背景、字号和字重对齐系统分屏/浮窗，文字上方为 24 dp 图标。
- 有工作台时把当前全屏任务追加为主窗；没有工作台时新建容器并接入该任务。满三窗时不显示中间项。
- 自 0.4.6-beta.1 起一并带上侧卡盘面、透视、壁纸背景，以及多任务返回后的图层恢复。

验证：本地布局回归通过。上滑接入已在一加 13 上做过有限验收；本次只确认 beta.22 已安装，不把全部手势回归视为通过。仅列为测试版本。

## v0.4.5-beta.1

系统返回第一阶段。

- 键盘、弹窗和内部页面优先交给主应用处理；取消手势不提交。
- 主应用根页面返回桌面并保留当前组合，不移出单个应用，也不解散工作台。
- 入口区分“打开 / 恢复当前工作台”和“用所选应用新建工作台”。
- 菜单移出与“退出工作台”保持独立。
- 尚未按来源任务、新 Activity、新任务和跨应用跳转复刻全部原版返回语义。

验证：一加 13 / ColorOS 16 真机完成 IME、内部返回、取消手势、三窗/双窗/单窗根页返回恢复，以及明确退出。仅列为测试版本。

## v0.4.4-beta.2

横屏应用按原版方式旋转显示。

- 竖握时横屏任务顺时针旋转 90° 成为长卡片；物理横屏时恢复正向。
- 左右布局改为左侧窄预览栏、右侧主窗；上下为顶部预览、下方主窗。
- 新增卡片几何计算，主卡按内容比例居中，侧卡按各自渲染比例缩短。

验证：两种布局预览切换、基础触控、设备旋转、键盘、Home 返回、滑动移出和整组退出。仅列为测试版本。

## v0.4.3-beta.1

首次 GitHub Beta / Pre-release 发布。

- 应用名称：多窗工作台（WindowDeck）。
- 包名与源码命名空间：`dev.windowdeck.app`。
- 同步 LSPosed 入口、启动参数、日志标签及 APK 命名。
- 基于 0.4.3：三应用实时工作台、主侧窗切换、滑动移出、本地固定，以及各应用独立的渲染方向。
- 保留 0.4.3 的横屏应用显示方式；不包含撤销的横屏模式切换改动。
- 提供布局、手势与方向测试及可配置依赖路径的构建脚本。

验证：本地构建、签名校验及纯 Java 回归测试通过。仅列为测试版本。
